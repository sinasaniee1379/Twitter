import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles({
  root: {
    background: "#e6e6e6",
    flex: 1,
    overflowY: "auto",
  },
  divider: {
    background: "#7ebaff",
    filter: "opacity(.2)",
  },
  hashimg: {
    width: "30",
    height: "32px",
  },
});

export default useStyles;
